spring:
  application:
    name: TaskManager
  datasource:
    username: postgres
    password: ${PASSW}
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/task
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true